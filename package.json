{
  "name": "herdr-layout",
  "version": "0.0.2",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "dev": "bun src/index.ts",
    "build:release": "bun scripts/build-release.ts",
    "build:release:all": "bun scripts/build-release.ts --all"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
